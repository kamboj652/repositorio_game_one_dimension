<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gameonedimension.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name">
        <activity
            android:name=".OneDimensionvCActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".dimensions.OneDimensionFIN" />
        <activity android:name=".dimensions.OneDimensionJuego" />
        <activity android:name=".dimensions.OneDimensionRespuesta" />
        <activity android:name=".dimensions.OneDimensionSolucion" />
    </application>

</manifest>